#include <iostream>

using namespace std;

int main()
{
    int n, temp, raio;
    cin >> n;
    for (int i = 0; i < n; i++){
        cin >> temp;
        if(temp > raio){
            raio = temp;
        }
    }
    cout << 3*raio*raio << endl;
    return 0;
}
